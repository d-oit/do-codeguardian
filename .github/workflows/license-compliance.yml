name: License Compliance

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - name: Test
      run: echo "License compliance workflow is working!"
